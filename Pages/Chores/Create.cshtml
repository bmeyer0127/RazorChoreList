@page
@model RazorChoreList.Pages_Chores.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Chore</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Chore.ChoreName" class="control-label"></label>
                <input asp-for="Chore.ChoreName" class="form-control" />
                <span asp-validation-for="Chore.ChoreName" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* <label asp-for="Chore.ChoreDoer" class="control-label"></label>
                <input asp-for="Chore.ChoreDoer" class="form-control" />
                <span asp-validation-for="Chore.ChoreDoer" class="text-danger"></span> *@
                <label asp-for="Chore.People.name" class="control-label"></label>
            </div>
            <div class="form-group">
                <label asp-for="Chore.CompletionStatus" class="control-label"></label>
                <div>
                    @* Remove foreach and manually add both options and have CompletinoStatusOptions[0] be checked *@
                    @foreach (var option in Chore.CompletionStatusOptions)
                    {
                        <input type="radio" asp-for="Chore.CompletionStatus" value=@option /> @option<br />
                    }
                </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
